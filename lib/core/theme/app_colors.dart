import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000); // Deep Black
  static const Color primary = Color(0xFFD32F2F);    // Bold Red
  static const Color accent = Color(0xFFFFFFFF);     // Crisp White
  static const Color surface = Color(0xFF1E1E1E);    // Dark grey for cards
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
}
